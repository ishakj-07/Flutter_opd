 /*String name = _nameController.text.trim();
                    if (name.isEmpty) {
                      ScaffoldMessenger.of(context).showSnackBar(
                        SnackBar(
                          content: Text("Please enter the patient's name."),
                          backgroundColor: Colors.redAccent,
                        ),
                      );
                    } else {
                      // You can add logic to save the patient to a list/database
                      ScaffoldMessenger.of(context).showSnackBar(
                        SnackBar(
                          content: Text("Patient '$name' added successfully!"),
                          backgroundColor: Colors.green,
                        ),
                      );
                      // Optional: clear the field
                      _nameController.clear();

                      openDialogueBox();
                    }
                  },
                  style: ElevatedButton.styleFrom(
                    backgroundColor: Color(0xFF738ED3),
                    shape: RoundedRectangleBorder(
                      borderRadius: BorderRadius.circular(10),
                    ),
                    padding: EdgeInsets.symmetric(horizontal: 24, vertical: 12),
                  ),*/